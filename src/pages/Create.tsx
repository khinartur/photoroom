import {observer} from 'mobx-react-lite'
import {PageLayout} from '../ui-kit/PageLayout'

export const CreatePage = observer(() => {
    return <PageLayout title="Create">Create page content</PageLayout>
})
